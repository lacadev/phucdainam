/**
 * Project List Block — frontend JS
 * Tabs nay là <a href> điều hướng URL, không cần JS filter client-side.
 * File giữ lại để không break import trong index.js.
 */
export function initProjectListBlock() {
	// no-op: filtering is handled server-side via ?project_cat= query string
}
